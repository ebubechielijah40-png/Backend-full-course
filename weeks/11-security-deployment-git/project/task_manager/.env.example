# Copy this file to .env for local development, or set these as real
# environment variables in your hosting platform's dashboard for
# production. Never commit an actual .env file.

DJANGO_SECRET_KEY=replace-with-a-long-random-value
DJANGO_DEBUG=False
DJANGO_ALLOWED_HOSTS=yourapp.example.com

DB_NAME=task_manager
DB_USER=task_manager_user
DB_PASSWORD=replace-with-a-real-password
DB_HOST=localhost
DB_PORT=5432
