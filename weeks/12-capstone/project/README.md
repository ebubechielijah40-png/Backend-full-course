# See ../../../capstone/README.md

The capstone project specification lives at the repository root
(`capstone/README.md`) rather than here, since it's referenced from
`ASSESSMENT.md` and the root `README.md` as the course's final
deliverable, not just Week 12's local project.

This folder is intentionally otherwise empty — there is no reference
solution for the capstone, since its entire purpose is to assess
independent, unassisted work (see `AI_USAGE_POLICY.md`, Stage 3).
