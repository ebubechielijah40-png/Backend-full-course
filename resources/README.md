# Shared Resources

This folder holds reference material that's used across more than one
week, so it doesn't need to be duplicated inside individual lessons (per
`STYLE_GUIDE.md`).

Nothing is filed here yet. Candidates to add as the detailed lessons are
written (only if genuinely reused across weeks — don't add material here
"just in case"):

- A shared list of common terminal commands used throughout the course.
- A PostgreSQL / `psql` quick-reference used by Weeks 5, 6, and 11.
- A Git command quick-reference used from Week 1 onward.

Do not duplicate this material inside lesson files once it exists here —
link to it instead.
